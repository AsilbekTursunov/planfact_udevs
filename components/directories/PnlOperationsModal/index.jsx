import React from 'react'
import CustomModal from '../../shared/CustomModal'

const PnlOperationsModal = ({ open, onClose }) => {
  return (
    <CustomModal open={open} onClose={onClose}>
      
    </CustomModal>
  )
}

export default PnlOperationsModal